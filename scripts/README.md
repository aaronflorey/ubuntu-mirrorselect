# Scripts

- `install-latest-release.ab`: Detects the current host OS and architecture, resolves the latest GitHub release for this repository, and installs `mirrorselect` into `~/.local/bin`.
- `../install.sh`: Compiled from `install-latest-release.ab` with `amber build` for users who want a plain shell installer.
